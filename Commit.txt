
** REFATORAÇÃO: Melhorias na comunicação com o microcontrolador
** Modificação da função uC_ReceberDados() em myscripts.js:
   - Correção do retorno quando não há dados: `return;` → `return null;`
   - Adição de bloco `else` para retornar explicitamente os dados recebidos
   - Adição de `return null;` no catch para tratamento de erros
   - Restruturação lógica com comentários de código antigo
** Atualização do script.js:
   - Adição de comentário sobre elementos KPI
   - Modificação do evento click do "Iniciar Preparação" para usar setInterval assíncrono
   - Implementação de loop (1000ms) para atualização contínua dos dados do microcontrolador
   - Exibição dinâmica dos dados de entrada, saída e desperdício nos KPIs
** Melhorias no server.js:
   - Adição dos comentários explicativos sobre a função run() e seu estado comentado
   - Adição da função uC_EnviarDados() para envio de comandos ao microcontrolador
   - Chamada automática de uC_EnviarDados({ cmd: 'start' }) na inicialização do servidor
arquivos afetados: /v_final2/myscripts.js, /v_final2/script.js, server.js
** Objetivo: Estabelecer comunicação bidirecional automática com o microcontrolador logo após o início da produção